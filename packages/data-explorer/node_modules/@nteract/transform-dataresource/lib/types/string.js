"use strict";

var _interopRequireDefault = require("@babel/runtime-corejs2/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.castString = castString;

var _lodash = _interopRequireDefault(require("lodash"));

var _error = require("./error");

// Module API
function castString(format, value) {
  if (!_lodash.default.isString(value)) {
    return _error.ERROR;
  }

  if (format === "uri") {
    if (!_URI_PATTERN.exec(value)) {
      return _error.ERROR;
    }
  } else if (format === "email") {
    if (!_EMAIL_PATTERN.exec(value)) {
      return _error.ERROR;
    }
  } else if (format === "uuid") {// TODO: implement validation
  } else if (format === "binary") {// TODO: implement validation
  }

  return value;
} // Internal


var _URI_PATTERN = new RegExp("^http[s]?://");

var _EMAIL_PATTERN = new RegExp("[^@]+@[^@]+\\.[^@]+");