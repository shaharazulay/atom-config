"use strict";

var _interopRequireDefault = require("@babel/runtime-corejs2/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.castGeopoint = castGeopoint;

var _slicedToArray2 = _interopRequireDefault(require("@babel/runtime-corejs2/helpers/slicedToArray"));

var _lodash = _interopRequireDefault(require("lodash"));

var _error = require("./error");

// Module API
function castGeopoint(format, value) {
  var lon, lat;

  try {
    if (format === "default") {
      if (_lodash.default.isString(value)) {
        var _value$split = value.split(",");

        var _value$split2 = (0, _slicedToArray2.default)(_value$split, 2);

        lon = _value$split2[0];
        lat = _value$split2[1];
        lon = lon.trim();
        lat = lat.trim();
      } else if (_lodash.default.isArray(value)) {
        var _value = value;

        var _value2 = (0, _slicedToArray2.default)(_value, 2);

        lon = _value2[0];
        lat = _value2[1];
      }
    } else if (format === "array") {
      if (_lodash.default.isString(value)) {
        value = JSON.parse(value);
      }

      var _value3 = value;

      var _value4 = (0, _slicedToArray2.default)(_value3, 2);

      lon = _value4[0];
      lat = _value4[1];
    } else if (format === "object") {
      if (_lodash.default.isString(value)) {
        value = JSON.parse(value);
      }

      lon = value.lon;
      lat = value.lat;
    }

    lon = _lodash.default.toNumber(lon);
    lat = _lodash.default.toNumber(lat);
  } catch (error) {
    return _error.ERROR;
  }

  if (_lodash.default.isNaN(lon) || lon > 180 || lon < -180) {
    return _error.ERROR;
  }

  if (_lodash.default.isNaN(lat) || lat > 90 || lat < -90) {
    return _error.ERROR;
  }

  return [lon, lat];
}