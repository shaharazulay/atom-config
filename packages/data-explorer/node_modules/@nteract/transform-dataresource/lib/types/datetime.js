"use strict";

var _interopRequireDefault = require("@babel/runtime-corejs2/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.castDatetime = castDatetime;

var _lodash = _interopRequireDefault(require("lodash"));

var _moment = _interopRequireDefault(require("moment"));

var _d3TimeFormat = require("d3-time-format");

var _error = require("./error");

// Module API
function castDatetime(format, value) {
  if (!_lodash.default.isDate(value)) {
    if (!_lodash.default.isString(value)) {
      return _error.ERROR;
    }

    try {
      if (format === "default") {
        value = (0, _moment.default)((0, _d3TimeFormat.timeParse)(_DEFAULT_PATTERN)(value));
      } else if (format === "any") {
        value = (0, _moment.default)(value);
      } else {
        if (format.startsWith("fmt:")) {
          console.warn("Format \"fmt:<PATTERN>\" is deprecated.\n             Please use \"<PATTERN>\" without \"fmt:\" prefix.");
          format = format.replace("fmt:", "");
        }

        value = (0, _moment.default)((0, _d3TimeFormat.timeParse)(format)(value));
      }

      if (!value.isValid()) {
        return _error.ERROR;
      }

      value = value.toDate();
    } catch (error) {
      return _error.ERROR;
    }
  }

  return value;
} // Internal


var _DEFAULT_PATTERN = "%Y-%m-%dT%H:%M:%SZ";